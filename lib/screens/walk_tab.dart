import 'package:flutter/material.dart';

class WalkTab extends StatelessWidget {
  const WalkTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('산책로 추천 리스트'));
  }
}